#pragma once

#include <windows.h>

namespace nativetouch::settings {
    extern bool EMULATE_DIGITIZER;
    extern bool REFRESH_CONTACT_LIFETIME_FROM_GAME_LOOP;
    extern bool USE_GAME_CLIENT_COORDINATES;
}